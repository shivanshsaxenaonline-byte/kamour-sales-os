-- The tasks this created are ordinary `due` work items; they stay with their
-- reps. Only the combined entry point goes.
drop function if exists public.fn_assign_rrr_customers_with_work(uuid[], uuid);
